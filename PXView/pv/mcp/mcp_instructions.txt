PXView is a multi-mode signal analyzer with 4 work modes.
This server provides 45 tools organized in 4 tiers.

## Work Modes

PXView operates in one of four work modes. You MUST switch to the desired
mode before configuring channels, triggers, or probes:

  0 = Logic  — Digital signal analysis (digital channels, logic triggers,
               glitch filter, RLE compression, digital threshold)
  1 = Analog — Analog/DAQ acquisition (analog channels, probe config,
               math/spectrum/lissajous)
  2 = DSO    — Digital Storage Oscilloscope (oscilloscope channels, DSO
               triggers with source/slope/horizPos, probe config, math/
               spectrum/lissajous)
  3 = MSO    — Mixed Signal Oscilloscope (both digital + analog channels,
               all features available)

## Default Behavior

If the user does not specify a work mode, default to Logic mode (0).
Logic mode covers the most common use cases: digital signal analysis,
protocol decoding (SPI, I2C, UART, CAN, etc.), and digital triggers.

Do NOT ask the user which mode to use. Instead, infer from context:
- User mentions oscilloscope/probe/vdiv/coupling/math/spectrum → DSO mode (2)
- User mentions analog/DAQ/sensor → Analog mode (1)
- User explicitly needs mixed digital + analog → MSO mode (3)
- Otherwise → Logic mode (0), proceed silently

Only ask the user to clarify if their request is genuinely ambiguous
across multiple modes and the correct choice cannot be inferred.

## Standard Workflow

1. get_devices                    — discover available devices and their IDs
2. create_session(deviceId)       — create a session for the device
3. get_supported_work_modes       — check which modes the device supports
4. switch_work_mode(mode)         — select the work mode (MUST do before
                                     configuring channels/triggers/probes)
5. get_channels                   — see available channels for current mode
6. configure_channel              — enable/disable channels, set names
7. set_sample_config              — set sample rate/limit/timebase/collect mode
8. configure_trigger              — set trigger (auto-dispatches to LogicTrigger
                                     or DsoTrigger based on current mode)
9. configure_probe                — set probe/vdiv/coupling (DSO/Analog/MSO only)
10. configure_glitch_filter       — set glitch filter (Logic/MSO only)
11. add_analyzer                  — add protocol decoder (Logic/MSO only,
                                     before start_capture for auto-decode)
12. start_capture                 — begin acquisition (trigger config set separately)
13. wait_capture                  — wait for completion (non-stream mode)
14. get_samples                   — read raw samples (channelType must match mode)
15. get_analyzer_results          — read decoded protocol data
16. export_raw_data / export_data_table_csv — export results

## Mode-Specific Constraints

These tools will return an error if called in the wrong mode:

  configure_probe:        Only in DSO/Analog/MSO mode
  configure_glitch_filter: Only in Logic/MSO mode
  add_analyzer:           Only in Logic/MSO mode (decoders need digital channels)
  get_measurement_results: Only in DSO/Analog/MSO mode (math/spectrum/lissajous)

The error message will tell you which mode to switch to.

## Capture Modes

start_capture has TWO independent mode parameters:

  captureMode (collect mode — how captures repeat):
    'manual' or 'single' : one-shot capture (default)
    'repeat'             : repeated captures, repeatIntervalSeconds between
    'loop'               : loop mode

  channelMode (hardware acquisition mode — how the buffer fills):
    'Buffer' (default)   : traditional fixed-length capture. Use
                           durationSeconds for a timed capture.
    'Stream'             : continuous acquisition, no sample limit.
                           durationSeconds and sampleCount are IGNORED.
                           Call stop_capture to end streaming.

## Generic Config Keys (get_config / set_config)

The 'key' parameter is the numeric SR_CONF_* value. Common keys and the
required 'type' (a mismatched type fails — retry with another type):

  Acquisition / sampling
    30000 SAMPLERATE        uint64  (Hz)
    50001 LIMIT_SAMPLES     uint64  (sample count)
    50000 LIMIT_MSEC        uint64  (milliseconds)
    30001 CAPTURE_RATIO     uint64  (trigger position 0-100)
    60001 LOOP_MODE         bool
    60022 INSTANT           bool
    30003 RLE               bool    (compression, Logic mode)
    30002 PATTERN_MODE      string  (demo pattern, e.g. 'random')

  DSO / probe (per-channel keys target the channel given by channelIndex
  in dedicated tools; generic set_config applies device/group level)
    30010 TIMEBASE          uint64  (nanoseconds)
    30008 HORIZ_TRIGGERPOS  double  (percentage 0-100)
    30007 TRIGGER_SOURCE    int     (0=Auto, 1=CH0, 2=CH1)
    30004 TRIGGER_SLOPE     int     (0=Rising, 1=Falling)
    60035 PROBE_VDIV        uint64  (millivolts per division)
    40005 PROBE_FACTOR      uint64  (probe attenuation, e.g. 10 = 10x)
    60036 PROBE_COUPLING    int     (0=GND, 1=DC, 2=AC)
    60044 PROBE_MAP_DEFAULT bool

  Logic device options (PXLogic)
    60027 OPERATION_MODE    string
    60029 CHANNEL_MODE      string  ('Buffer' / 'Stream')
    60033 VTH               double  (logic threshold volts)
    30011 FILTER            string
    30041 CLOCK_EDGE        string
    60003 TRIGGER_OUT       bool
    60004 PWM0_EN / 60007 PWM1_EN        bool
    60005 PWM0_FREQ / 60008 PWM1_FREQ    double (Hz)
    60006 PWM0_DUTY / 60009 PWM1_DUTY    double (percent)

Prefer dedicated tools when one exists (set_sample_config,
configure_probe, configure_trigger) — they also keep the GUI in sync.

## Tool Organization (45 tools)

  Tier 0: Mode management (3 tools) — call first
    get_supported_work_modes, get_work_mode, switch_work_mode

  Tier 1: Core workflow (17 tools)
    get_devices, start_capture, stop_capture, wait_capture,
    get_capture_status, load_capture, save_capture, close_capture,
    add_analyzer, remove_analyzer, list_analyzers, get_analyzer_options,
    get_analyzer_results, export_raw_data, export_data_table_csv,
    get_channels, refresh_device_list

  Tier 2: Configuration (12 tools)
    set_sample_config, configure_channel, configure_trigger,
    configure_probe, configure_glitch_filter, configure_signal_invert,
    get_config, set_config, set_export_config, connect_device,
    disconnect_device, get_session_status

  Tier 3: Advanced features (13 tools)
    get_samples, find_next_edge, find_pattern, get_active_decoders,
    clear_all_decoders, reconfigure_decoder,
    list_sessions, create_session, destroy_session, set_active_session,
    get_measurement_results, configure_error_state, configure_cursors

## Tools That No Longer Exist (use the replacement instead)

  These were removed in an earlier consolidation. Do NOT call them:
    get_sample_config        -> get_session_status with include="config"
    get_decoder_class_names  -> get_analyzer_results with includeMetadata=true
    get_math_results         -> get_measurement_results with types=["math"]
    get_spectrum_results     -> get_measurement_results with types=["spectrum"]
    get_lissajous_results    -> get_measurement_results with types=["lissajous"]
    set_save_range           -> set_export_config (renamed, same parameters)

## Common Error Patterns

  "No active session": Call create_session or connect_device first.
  "Capture in progress": Call stop_capture or wait_capture first.
  "No data": Capture hasn't completed or no channels were enabled.
  "Config not supported": The device doesn't support the requested config.
  "Probe config not available in Logic mode": Call switch_work_mode(1) or (2).
  "Glitch filter not available in DSO mode": Call switch_work_mode(0).

## Sessions

PXView supports multiple sessions. Each session has its own device,
channels, and capture state. Use create_session to create a new one,
set_active_session to switch between them, and list_sessions to see all.
